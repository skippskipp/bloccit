<h1>New Ad</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for ads</p>
    <ul>
      <li>No ads related to pet psychology.</li>
      <li>No ads for dieting 'solutions'.</li>
      <li>No meta ads. Sell real things, not more ads.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @advertisement do |f| %>
      <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter advert body" %>
      </div>
      <div class="form-group">
        <%= f.label :price %>
        <%= f.text_area :price, rows: 1, class: 'form-control', placeholder: "Enter the price of your thing, you capitalist you." %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
