<h1>Edit Question</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Questions</p>
    <ul>
      <li>Remember to ask an actual question.</li>
      <li>No existential questions, they clog the db.</li>
      <li>No meta questions. Ask questions about things, not about 'questions'.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |f| %>
      <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter question body" %>
      </div>
      <div class="form-group">
        <%= f.label :resolved %>
        <%= check_box "question", "resolved", {}, "1", "0" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
      <% end %>
  </div>
</div>
